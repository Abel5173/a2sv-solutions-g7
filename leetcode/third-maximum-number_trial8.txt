class Solution:
    def thirdMax(self, nums: List[int]) -> int:
        n = list(set(nums))
        n.sort()
        if len(n) <= 2:
            return max(n)
        if len(n) <= 2:
            return max(n)
        return n[-3]