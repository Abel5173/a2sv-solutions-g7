class Solution:
    def rotate(self, nums: List[int], k: int) -> None:
        left = 0
        n = len(nums)
        while left<k:
            nums.insert(0,nums[n-1])
            nums.pop()
            left += 1